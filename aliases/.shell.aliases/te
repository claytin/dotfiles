# Choose a text editor
export EDITOR=$(which emacs vim vi nano 2>/dev/null | head -1)

function te {
	# handle emacs' special case, since it launches a gui by default
	if [ $(basename $EDITOR) = "emacs" ]; then
		NO_GUI="-nw"
	fi

	# Calls the default (t)ext (e)ditor
	$EDITOR $NO_GUI "$@"
}
